pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // NewPipeExtractor (Rat Tool) se sirve desde JitPack
        maven { url 'https://jitpack.io' }
    }
}
rootProject.name = "Naviris"
include(":app")

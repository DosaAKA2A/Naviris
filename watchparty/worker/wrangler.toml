name = "naviris-party"
# Cuenta de Studio Iris. Sin esto, un CLOUDFLARE_API_TOKEN/ACCOUNT_ID del entorno
# (el de la cuenta personal) manda el deploy a la cuenta equivocada (paso el
# 2026-09-09: quedo un worker suelto que hubo que borrar).
account_id = "3c4b0210e56bcb194140be2d6a491cc9"
main = "worker.js"
compatibility_date = "2026-07-01"

[[durable_objects.bindings]]
name = "PARTY"
class_name = "PartyRoom"

# new_sqlite_classes: los Durable Objects con almacenamiento SQLite son los
# que entran en el plan gratuito de Workers.
[[migrations]]
tag = "v1"
new_sqlite_classes = ["PartyRoom"]
